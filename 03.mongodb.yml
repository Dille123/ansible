---
- name: mongodb install and run
  hosts: mongodb
  become: yes # sudo access
  tasks:
  - name: install nginx server
    ansible.builtin.package:
      name: mongodb
      state: present
  
  - name: start mongodb
    ansible.builtin.service:
      name: mongodb
      state: started
      enabled: yes